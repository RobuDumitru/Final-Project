<UserControl x:Class="LostInAForgottenCity.Controls.StaminaSleepBar"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal"
                    VerticalAlignment="Center">
            <TextBlock Text="STAMINA"
                       FontFamily="Courier New" FontSize="11"
                       Foreground="#60aa60"/>
            <TextBlock x:Name="StaminaLabel" Text="0/0"
                       FontFamily="Courier New" FontSize="10"
                       Foreground="#60aa60"
                       Margin="8,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal"
                    VerticalAlignment="Center" HorizontalAlignment="Right">
            <TextBlock Text="SLEEP"
                       FontFamily="Courier New" FontSize="11"
                       Foreground="#6060aa"/>
            <TextBlock x:Name="SleepLabel" Text="0/0"
                       FontFamily="Courier New" FontSize="10"
                       Foreground="#6060aa"
                       Margin="8,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" x:Name="StaminaPanel"
                        Orientation="Horizontal"/>
            <Rectangle Grid.Column="1" Width="1" Fill="#3a3a4a" Margin="6,0"/>
            <StackPanel Grid.Column="2" x:Name="SleepPanel"
                        Orientation="Horizontal" Margin="0,0,0,0"/>
        </Grid>

        <Rectangle Grid.Row="2" Grid.ColumnSpan="2" Height="1"
                   Fill="#3a3a4a" Margin="0,8,0,0"/>
    </Grid>
</UserControl>